using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class InventoryCondition : MonoBehaviour
{
   /*public List<Inventory> inventoryList = new List<Inventory>();
    private void Awake()
    {
        PlayerInventoryGenerate.InventoryOpen  += InventoryStatusOpen;
        BeltInventoryGenerate.InventoryOpen    += InventoryStatusOpen;
        //InventoryGenerate.InventoryClose += InventoryStatusClose;
    }

    public void OnDisable()
    {
        PlayerInventoryGenerate.InventoryOpen  -= InventoryStatusOpen;
        for (int i = 0; i < inventoryList.Count; i++)
        {
            if (inventoryList[i].name != "BeltInventory")
                inventoryList.Remove(inventoryList[i]);
        }
       
    }

    void InventoryStatusOpen(Inventory inventory)
    {
        if (!inventoryList.Contains(inventory))
            inventoryList.Add(inventory);
      
    }*/
}
